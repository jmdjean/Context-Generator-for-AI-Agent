---
description: Project context for Context-Generator-for-AI-Agent — generated by AI Project Docs from the PKM
alwaysApply: true
---

# AI Project Docs — Cursor rule

<!-- Generated by AI Project Docs. Safe to update. -->

Derived from the Project Knowledge Model (PKM). Do not treat this file as authoritative over the PKM snapshot.

## Project summary

- Project: Context-Generator-for-AI-Agent
- Exported: 2026-07-11T00:40:24.779Z
- PKM assembled: 2026-07-11T00:40:24.740Z
- PKM schema: 1.0.0
- Analysis status: complete
- Languages: TypeScript
- Frameworks: None detected
- Package managers: npm
- Tooling: TypeScript
- Detection confidence: high
- Planned documents: 12
- Folder contexts: 39
- Modules: 12
- Conventions: 31
- Navigation entries: 8

## Mandatory reading order

1. `.ai-docs/AGENTS.md`
2. `.ai-docs/architecture.md`
3. `.ai-docs/agent-navigation.md`
4. `.ai-docs/folder-structure.md`
5. `.ai-docs/conventions.md`

Then load `.ai-docs/agent-navigation.md` to pick task-specific documents instead of reading everything.

For machine-readable context, start with `.ai-docs/knowledge/project-knowledge.json`.

## Architecture boundaries

Respect folder classifications and responsibilities. Do not move logic across boundaries without checking module and dependency impact.

### Source

- `src` — Contains the main source code of the project.
- `src/ai` — Integrates with AI providers for architecture analysis.
- `src/ai/providers` — Contains providers application source code.
- `src/analyzers` — Derives higher-level architectural knowledge from the PKM.
- `src/config` — Resolves runtime configuration from CLI flags and environment variables.
- `src/core` — Contains pipeline orchestration and core application coordination logic.
- …and 20 more source folders

### Test

- `test` — Contains automated tests for the project.
- `test/fixtures/fixture-typescript/tests` — Contains automated tests for the project.

### Config

- `` — Project root containing top-level configuration, documentation, and source entry points.
- `test/fixtures/fixture-monorepo` — Contains project configuration files.
- `test/fixtures/fixture-monorepo/apps/api` — Contains project configuration files.
- `test/fixtures/fixture-monorepo/apps/web` — Contains project configuration files.
- `test/fixtures/fixture-monorepo/libs/shared` — Contains project configuration files.
- `test/fixtures/fixture-typescript` — Contains project configuration files.

### Documentation

- `docs` — Contains human-readable project documentation.
- `test/fixtures/fixture-minimal` — Contains human-readable project documentation.

### Scripts

- `scripts` — Contains automation and utility scripts.

### Unknown

- `test/fixtures` — Contains project files related to fixtures.
- `test/fixtures/fixture-typescript/src/utils` — Contains project files related to utils.

### Architectural conventions

- **Deterministic analyzers under src/analyzers** (high) — Deterministic PKM enrichment analyzers live under src/analyzers.
- **Documentation generation under src/docs** (high) — Documentation planning and writing live under src/docs.
- **Domain types under src/domain** (high) — Pure domain types and the declarative pipeline live under src/domain.
- **Pipeline orchestration under src/core** (high) — Pipeline orchestration and step handlers live under src/core.
- **PKM under src/knowledge** (high) — The Project Knowledge Model types, builder, and persistence live under src/knowledge.
- **Repository scanning under src/scanner** (high) — Repository loading and scanning live under src/scanner.
- **Source code under src/** (high) — Application source code lives under the src/ directory.
- **Technology detection under src/detectors** (high) — Technology detection lives under src/detectors.
- **src/analyzers consume the PKM** (high) — Analyzers enrich the PKM deterministically without re-scanning the repository.
- **src/core orchestrates the pipeline** (high) — The core module drives pipeline execution and coordinates every stage.
- **src/detectors feed upstream knowledge** (high) — Detectors identify the technology stack that informs downstream analysis.
- **src/docs writes derived output** (high) — The docs module plans and renders documentation generated from the PKM.
- **src/domain remains pure** (high) — The domain module contains only types and declarative data — no runtime behavior.
- **src/knowledge owns the PKM** (high) — The knowledge module defines, assembles, and persists the Project Knowledge Model.
- **src/scanner feeds upstream knowledge** (high) — The scanner reads the repository from disk and supplies the structural baseline.

## Module map

| Module | Type | Responsibility | Confidence |
|---|---|---|---|
| `.ai-docs` | documentation | Contains AI-readable project context generated by this tool. | high |
| `docs` | documentation | Contains human-readable project documentation. | high |
| `src/ai` | core | Integrates with AI providers for architecture analysis. | high |
| `src/analyzers` | core | Derives higher-level architectural knowledge from the PKM. | high |
| `src/config` | configuration | Resolves runtime configuration from CLI flags and environment variables. | high |
| `src/core` | core | Contains pipeline orchestration and core application coordination logic. | high |
| `src/detectors` | core | Detects technologies and tooling from repository metadata. | high |
| `src/docs` | core | Plans and writes documentation outputs derived from project knowledge. | high |
| `src/domain` | core | Defines pure domain types and the declarative analysis pipeline. | high |
| `src/knowledge` | core | Represents and persists the Project Knowledge Model used as the source of truth. | high |
| `src/scanner` | core | Builds the repository tree used by analyzers. | high |
| `src/utils` | tooling | Provides shared utility functions with no domain knowledge. | high |

## Convention summary

- **TypeScript project** (language, high) — The project is written in TypeScript with a root tsconfig.json.
- **TypeScript strict mode** (language, high) — The TypeScript compiler runs with strict mode enabled; all code must satisfy strict checks.
- **Dependencies managed with npm** (package-management, high) — Dependencies are installed and scripts are run with npm.
- **Deterministic analyzers under src/analyzers** (repository-structure, high) — Deterministic PKM enrichment analyzers live under src/analyzers.
- **Documentation generation under src/docs** (repository-structure, high) — Documentation planning and writing live under src/docs.
- **Domain types under src/domain** (repository-structure, high) — Pure domain types and the declarative pipeline live under src/domain.
- **Pipeline orchestration under src/core** (repository-structure, high) — Pipeline orchestration and step handlers live under src/core.
- **PKM under src/knowledge** (repository-structure, high) — The Project Knowledge Model types, builder, and persistence live under src/knowledge.
- **Repository scanning under src/scanner** (repository-structure, high) — Repository loading and scanning live under src/scanner.
- **Source code under src/** (repository-structure, high) — Application source code lives under the src/ directory.
- **Technology detection under src/detectors** (repository-structure, high) — Technology detection lives under src/detectors.
- **src/analyzers consume the PKM** (architecture, high) — Analyzers enrich the PKM deterministically without re-scanning the repository.
- …and 19 more in `.ai-docs/conventions.md`

## Navigation map by task type

Match your current task to an entry below, then load only the recommended PKM sections and documents.

### Architecture change

Changing module boundaries, pipeline structure, or how stages and generators interact.

- Recommended PKM knowledge: `modules`, `folderContexts`, `dependencyGraph`, `conventions`
- Recommended documents: `architecture.md`, `folder-structure.md`, `dependency-map.md`, `conventions.md`, `ai-context.md`
- Related modules: `src/analyzers`, `src/core`, `src/docs`, `src/domain`, `src/knowledge`
- Related folders: `src/analyzers`, `src/core`, `src/docs`, `src/domain`, `src/knowledge`
- Confidence: high

Warnings:
- Review dependency graph before changing module boundaries.
- Preserve domain purity.
- Keep generators consuming PKM instead of scanning directly.

### New feature

Adding new behavior: a pipeline step, analyzer, generator, detector, or CLI capability.

- Recommended PKM knowledge: `modules`, `folderContexts`, `conventions`, `dependencyGraph`, `navigationMap`
- Recommended documents: `architecture.md`, `folder-structure.md`, `implementation-guide.md`, `conventions.md`, `agent-navigation.md`
- Related modules: `src/analyzers`, `src/core`, `src/docs`, `src/domain`, `src/knowledge`
- Related folders: `src`
- Confidence: high

Warnings:
- Add new behavior through the correct module boundary.
- Update PKM-related docs if architecture changes.
- Do not bypass the pipeline orchestration layer.

### Bug fix

Correcting incorrect behavior in existing code without changing architecture or contracts.

- Recommended PKM knowledge: `modules`, `dependencyGraph`, `conventions`
- Recommended documents: `implementation-guide.md`, `dependency-map.md`, `conventions.md`
- Related modules: `src/ai`, `src/analyzers`, `src/core`, `src/detectors`, `src/docs`, `src/domain`, `src/knowledge`, `src/scanner`
- Related folders: None resolved
- Confidence: high

Warnings:
- Fix the root cause instead of patching generated output.
- Avoid changing unrelated modules.

### Test change

Adding, updating, or reorganizing tests for existing or new behavior.

- Recommended PKM knowledge: `conventions`, `modules`, `folderContexts`
- Recommended documents: `conventions.md`, `implementation-guide.md`
- Related modules: None resolved
- Related folders: `test`, `test/fixtures/fixture-typescript/tests`
- Confidence: high

Warnings:
- Preserve existing testing conventions.
- Do not weaken assertions or remove tests.

### Documentation change

Updating Markdown documentation, READMEs, or the generated context layer.

- Recommended PKM knowledge: `documentation`, `conventions`, `navigationMap`
- Recommended documents: `README.md`, `agent-navigation.md`, `ai-context.md`, `conventions.md`
- Related modules: `.ai-docs`, `docs`, `src/docs`, `src/knowledge`
- Related folders: `docs`, `test/fixtures/fixture-minimal`
- Confidence: high

Warnings:
- Markdown should reflect PKM.
- Do not manually edit generated files unless intentionally supported.

### Config change

Changing runtime configuration, compiler options, or tooling configuration files.

- Recommended PKM knowledge: `technologies`, `conventions`, `modules`
- Recommended documents: `architecture.md`, `conventions.md`, `implementation-guide.md`
- Related modules: `src/config`, `src/detectors`, `src/utils`
- Related folders: `test/fixtures/fixture-monorepo`, `test/fixtures/fixture-monorepo/apps/api`, `test/fixtures/fixture-monorepo/apps/web`, `test/fixtures/fixture-monorepo/libs/shared`, `test/fixtures/fixture-typescript`
- Confidence: high

Warnings:
- Check package manager and TypeScript strict mode conventions.
- Avoid introducing conflicting tooling.

### Dependency change

Adding, removing, or upgrading package dependencies.

- Recommended PKM knowledge: `technologies`, `dependencyGraph`, `conventions`
- Recommended documents: `dependency-map.md`, `conventions.md`, `implementation-guide.md`
- Related modules: `src/detectors`
- Related folders: `test/fixtures/fixture-monorepo`, `test/fixtures/fixture-monorepo/apps/api`, `test/fixtures/fixture-monorepo/apps/web`, `test/fixtures/fixture-monorepo/libs/shared`, `test/fixtures/fixture-typescript`
- Confidence: high

Warnings:
- Check impacted modules before changing dependencies.
- Keep dependency changes minimal.

### Ai agent integration

Building or adjusting agent-facing outputs: agent docs, exporters, rules, or skills.

- Recommended PKM knowledge: `documentation`, `conventions`, `navigationMap`, `modules`
- Recommended documents: `AGENTS.md`, `ai-context.md`, `agent-navigation.md`, `implementation-guide.md`
- Related modules: `.ai-docs`, `docs`, `src/analyzers`, `src/docs`, `src/knowledge`
- Related folders: `docs`, `test/fixtures/fixture-minimal`
- Confidence: high

Warnings:
- Agent-specific outputs must be derived from PKM.
- Do not duplicate repository analysis inside exporters.

## Dependency graph summary

- Nodes (modules): 10
- Edges (detected relationships): 27

High-confidence relationships:
- `src/ai` → `src/config` (imports)
- `src/ai` → `src/knowledge` (imports)
- `src/ai` → `src/utils` (imports)
- `src/analyzers` → `src/domain` (imports)
- `src/analyzers` → `src/knowledge` (imports)
- `src/analyzers` → `src/scanner` (imports)
- `src/analyzers` → `src/utils` (imports)
- `src/config` → `src/ai` (imports)
- …and 19 more

See `.ai-docs/dependency-map.md` for the full node and edge list with evidence.

## Safety rules

- Never modify project source files unless the user explicitly asks for code changes.
- Only write generated context inside `.ai-docs` or tool-managed agent export paths (such as `.cursor/rules/`).
- Do not overwrite user-managed files that lack the generated-file marker.
- Do not re-scan the repository or call OpenRouter from derived outputs — consume the PKM instead.
- Treat high-confidence conventions as hard constraints; verify low-confidence ones against code.
- Check the dependency graph before changing shared modules.
- When AI insights are present in the PKM, treat them as non-authoritative enrichment only.

## PKM is the source of truth

This Cursor rule is a derived presentation layer generated from the Project Knowledge Model (PKM).
Facts come from deterministic analyzers assembled once per pipeline run.

- Canonical snapshot: `.ai-docs/knowledge/project-knowledge.json`
- Split sections: `.ai-docs/knowledge/`

Markdown documents, agent packs, and agent-specific exports must never become the source of truth.
When outputs disagree, trust the PKM.
Regenerate with `ai-project-docs --export-agents --target cursor` after PKM sections change.
