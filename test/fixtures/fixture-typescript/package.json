{
  "name": "my-ts-app",
  "version": "1.0.0",
  "description": "A small TypeScript application fixture for testing",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/tests/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
